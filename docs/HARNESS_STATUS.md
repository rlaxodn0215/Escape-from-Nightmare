# Harness Development Status

## Current Build Focus
- Target scene flow: `TitleScene` -> `MainScene` -> `child_room` -> `second_floor_hallway` -> `study` -> `study_safe`, then `second_floor_bathroom`, `dressing_room`, `mirror_room`, `master_bedroom`, the attic toy chain, and the first-floor laundry power chain.
- Harness purpose: validate escape-room runtime flow before final room, puzzle, monster, and art production.
- Player-facing status/log UI is intentionally not shown. Runtime state changes should use Unity `Debug.Log`.

## Implemented Systems
- Escape runtime base:
  - `GameSession` tracks current room, current face, inventory, selected item, flags, solved puzzles, and used interactables.
  - `FlagService`, `PuzzleService`, `InteractionSystem`, and `EscapeActionResolver` handle conditions, puzzle solving, rewards, flags, and standard escape actions.
  - 4-face room navigation is active through left/right rotation.
  - `second_floor_hallway` is the current exception: it uses a 2-screen perspective hub and left/right swaps between the two hallway views.
- Sound:
  - `SoundManager` and `SoundCatalog` route BGM, SFX, UI, ambience, and monster categories.
  - Stage ambient BGM, UI click, item pickup, hide, puzzle, door, drawer open, and drawer close fallback sounds are registered.
- Title scene:
  - Image-based title background, logo, buttons, settings panel, and slider UI are set up.
  - Start targets `MainScene`.
- Main scene:
  - Direct scene view exists with camera, canvas, event system, audio listener, `GameDirector`, `SoundManager`, room view, close-up modal, hide view modal, and inventory window.
  - Existing scene references are also runtime-normalized by `GameDirector` so older scene layouts still behave correctly.

## Current `child_room` Harness
- Room structure:
  - `child_room` has four faces: North, East, South, West.
  - Current face starts on North when entering the room.
  - Moving rooms resets face direction to North.
- Interaction visibility:
  - World object sprites are hidden in the 4-face room view.
  - Transparent hitbox buttons remain for interaction.
- Face interactions:
  - North: `child_desk_drawer`
  - East: `child_room_door`
  - South: `child_bed_hide`
  - West: `child_window_silhouette`
- Desk drawer:
  - Opens a full-screen close-up instead of immediately granting the item.
  - Close-up states: closed, open with torn drawing fragment, open empty.
  - Drawer open, drawer close, and item pickup sounds are connected.
  - After `torn_drawing_fragment` is acquired, the room-level `child_desk_drawer` hitbox is disabled.
- Hide view:
  - `child_bed_hide` opens a full-screen under-bed view modal.
  - The latest image uses a low under-bed viewpoint with the top portion heavily covered by bed cloth/frame.
  - Back arrow exits the hide view and returns to the room face.
- Window:
  - West room image has the person silhouette removed.

## Current `second_floor_hallway` Harness
- Room structure:
  - `second_floor_hallway` has two perspective screens instead of the standard four faces.
  - North screen is the child-room-side view looking into the hallway.
  - South screen is the stairwell/inner-side view looking back toward the child room.
  - Left/right arrows toggle between North and South.
- Door layout:
  - North screen: `child_room`, `study`, `second_floor_bathroom`, `mirror_room`.
  - South screen: `master_bedroom`, `dressing_room`, `stairwell_2f`.
- Interaction visibility:
  - Hallway doors are painted into the background images.
  - Door objects use transparent hitboxes with `showWorldImage=false`.
- Visual continuity:
  - The child-room exit image and hallway child-room doorway were adjusted to share the same open-door direction and mood.

## Current `study` Harness
- Room structure:
  - `study` follows the standard four-face room rule.
  - North: safe/bookcase wall.
  - East: exit door back to `second_floor_hallway`.
  - South: desk and clue note.
  - West: moonlit window and portrait wall with no interaction in this milestone.
- Interaction visibility:
  - Study interactables are included in the background art.
  - `study_safe_obj`, `study_exit`, and `study_safe_clue_note` use transparent hitboxes with `showWorldImage=false`.
- Safe clue:
  - `study_safe_clue_note` opens a read-only close-up image through the shared close-up modal.
  - The clue image communicates the order `3-1-4-2` as separate aged marks/cards, not as one plain written code line.
- Safe puzzle:
  - `study_safe` remains `PuzzleType.NumberLock`.
  - Answer tokens are `3`, `1`, `4`, `2`.
  - Solving the number lock sets `study_safe_unlocked` and `study_safe_opened`, then opens the safe close-up.
  - `fuse_holder` is granted only from the open-safe item hotspot.
  - Picking up `fuse_holder` sets `puzzle_study_safe_clear` and marks `study_safe` solved.
  - Wrong answers do not grant the item, solved flag, or clear flag.
  - Safe digit UI uses image sprites only, not runtime number text.
  - Safe digit image/button RectTransform values are preserved from the editor scene when references already exist.
  - Open safe room states:
    - `study_safe_opened` and no `fuse_holder`: `Rooms/study_north_safe_open_with_item.png`
    - `fuse_holder` acquired: `Rooms/study_north_safe_open_empty.png`

## Current `second_floor_bathroom` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update`.
- Room structure:
  - `second_floor_bathroom` follows the standard four-face room rule.
  - North: mirror reversal clue.
  - East: exit door back to `second_floor_hallway`.
  - South and West are generated ambient bathroom faces with no interactions in this milestone.
- Interaction visibility:
  - `bathroom_mirror_rule_clue` and `bathroom_exit` use transparent hitboxes with `showWorldImage=false`.
- Mirror clue:
  - `bathroom_mirror_rule_clue` opens a read-only close-up image through the shared close-up modal.
  - Current image is generated first-pass art and communicates the mirror reversal rule visually without in-image text.
- Scope lock:
  - No hiding spot is added yet because the exact hiding location/visual is not final for this harness pass.

## Current `mirror_room` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update`.
- Room structure:
  - `mirror_room` follows the standard four-face room rule.
  - North: cracked mirror and `mirror_symbol_panel` puzzle panel.
  - East: exit door back to `second_floor_hallway`.
  - South and West are generated ambient mirror-room faces with no interactions in this milestone.
- Interaction visibility:
  - `mirror_panel_obj` and `mirror_exit` use transparent hitboxes with `showWorldImage=false`.
- Symbol panel:
  - `mirror_panel_obj` opens the existing `mirror_symbol_panel` puzzle.
  - Current puzzle close-up image is generated first-pass art and shows the heart, hand, cracked mirror circle, and keyhole symbols without in-image text or numbers.
- Scope lock:
  - Existing answer, reward, and success flag remain unchanged.
  - Mirror destruction, reward close-up states, and final solve presentation are deferred to a later pass.

## Current `dressing_room` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update`.
- Room structure:
  - `dressing_room` follows the standard four-face room rule.
  - North: wardrobe and color-sequence clue.
  - East: exit door back to `second_floor_hallway`.
  - South and West are generated ambient dressing room faces with no interactions in this milestone.
- Interaction visibility:
  - `dressing_color_sequence_clue` and `dressing_exit` use transparent hitboxes with `showWorldImage=false`.
- Color clue:
  - `dressing_color_sequence_clue` opens a read-only close-up image through the shared close-up modal.
  - Current image is generated first-pass art and communicates `black -> white -> red -> gray` with fabric colors, without in-image text or numbers.
- Scope lock:
  - No hiding spot is added yet because the exact hiding location/visual is not final for this harness pass.
  - `master_bedroom_drawer` runtime is connected; drawer open-state art remains deferred.

## Current `attic_toy_chain` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update + Generated bitmap assets`.
- Route:
  - `second_floor_hallway` South face -> `stairwell_2f`.
  - `stairwell_2f` East face -> `attic_main`.
  - `attic_main` East face -> `attic_toy_storage`.
  - `attic_toy_storage` North face -> `attic_toy_sequence`.
- Room structure:
  - `stairwell_2f`, `attic_main`, and `attic_toy_storage` each use four directional faces.
  - `stairwell_2f` North exits to `second_floor_hallway`, East exits to `attic_main`, South exits to `stairwell_1f`, and West is ambient.
  - `attic_main` North opens the `attic_family_album_photo` clue, East exits to `attic_toy_storage`, South exits to `stairwell_2f`, and West is ambient.
  - `attic_toy_storage` North opens `attic_toy_sequence`, East exits to `attic_main`, and South/West are ambient.
- Puzzle behavior:
  - `attic_toy_sequence` requires `torn_drawing_fragment`.
  - Correct order remains `doll -> train -> block -> bell`.
  - Success grants `small_doll` and `symbol_fragment`, sets `attic_toy_box_opened`, and is one-shot.
- Scope lock:
  - Attic hiding spots, final lore text, and monster-specific attic behavior are not locked in this pass.

## Current `first_floor_laundry_power_chain` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update + Generated bitmap assets`.
- Route:
  - `stairwell_2f` South face -> `stairwell_1f`.
  - `stairwell_1f` East face -> `first_floor_hallway`.
  - `first_floor_hallway` North screen -> `dining_room`, `kitchen`, and `laundry_room`.
- Room structure:
  - `stairwell_1f`, `dining_room`, `kitchen`, and `laundry_room` use four directional faces.
  - `first_floor_hallway` is a two-screen perspective hub like `second_floor_hallway`.
  - `dining_room` North opens `dining_seat_order_clue`; East exits to `first_floor_hallway`.
  - `kitchen` North opens `kitchen_clock_clue`; East exits to `first_floor_hallway`; South opens `kitchen_hide`.
  - `laundry_room` North opens `laundry_storage_box`; East opens `breaker_box`; South opens `laundry_hide`; West exits to `first_floor_hallway`.
- Puzzle behavior:
  - `laundry_storage_box` uses `0 -> 9 -> 1 -> 5`, grants `fuse`, sets `puzzle_laundry_box_clear`, and is one-shot.
  - `breaker_box` requires `fuse_holder` and `fuse`, grants `old_keychain`, sets `electricity_restored`, and is one-shot.
  - `breaker_box` does not consume `fuse_holder` or `fuse` in this harness pass.
- Scope lock:
  - Kitchen monster first appearance, electricity-noise monster behavior, basement electronic lock release, basement altar flow, and final chase are not locked in this pass.

## UI / Modal Behavior
- Close-up and hide screens are full-screen.
- Close/exit controls use a shared back-arrow image rather than an `X`.
- Modal open/close uses fade-in/fade-out through `CanvasGroup`.
- Room direction changes and room movement use the full-screen scene transition overlay fade.
- Puzzle close-ups, clue close-ups, and hide views use modal `CanvasGroup` fade and block raycasts while transitioning.
- Inventory is opened by a dedicated inventory button and shows item icons in slots.

## Assets And Builder
- Runtime resources live under `Assets/EscapeFromNightmares/Resources/EscapeFromNightmares`.
- `RoomSpriteCatalog` directly references room backgrounds, close-up images, hide view image, inventory UI, rotation buttons, back arrow, puzzle images, and item icons.
- `Escape From Nightmares/Rebuild Main Sample Assets` is responsible for regenerating scenes, prefabs, fallback images, fallback sounds, and catalog references.
- Known limitation: Unity batchmode builder/test execution can fail with exit code `1` when the project is already locked by an open editor. Close the editor before running batch validation.
- Recently added study resources:
  - `Rooms/study_north.png`
  - `Rooms/study_east.png`
  - `Rooms/study_south.png`
  - `Rooms/study_west.png`
  - `Rooms/study_north_safe_open.png`
  - `Rooms/study_north_safe_open_with_item.png`
  - `Rooms/study_north_safe_open_empty.png`
  - `Puzzles/study_safe.png`
  - `Puzzles/study_safe_digit_0.png` through `Puzzles/study_safe_digit_9.png`
  - `CloseUps/study_safe_clue_note.png`
  - `CloseUps/study_safe_open_with_item.png`
  - `CloseUps/study_safe_open_empty.png`
  - `Items/item_fuse_holder.png`
- Recently added bathroom resources:
  - `Rooms/second_floor_bathroom_north.png`
  - `Rooms/second_floor_bathroom_east.png`
  - `Rooms/second_floor_bathroom_south.png`
  - `Rooms/second_floor_bathroom_west.png`
  - `CloseUps/bathroom_mirror_rule_clue.png`
- Recently added dressing room resources:
  - `Rooms/dressing_room_north.png`
  - `Rooms/dressing_room_east.png`
  - `Rooms/dressing_room_south.png`
  - `Rooms/dressing_room_west.png`
  - `CloseUps/dressing_color_sequence_clue.png`
- Recently added mirror room resources:
  - `Rooms/mirror_room_north.png`
  - `Rooms/mirror_room_east.png`
  - `Rooms/mirror_room_south.png`
  - `Rooms/mirror_room_west.png`
  - `Puzzles/mirror_symbol_panel.png`
- Recently added attic resources:
  - `Rooms/stairwell_2f_north.png`
  - `Rooms/stairwell_2f_east.png`
  - `Rooms/stairwell_2f_south.png`
  - `Rooms/stairwell_2f_west.png`
  - `Rooms/attic_main_north.png`
  - `Rooms/attic_main_east.png`
  - `Rooms/attic_main_south.png`
  - `Rooms/attic_main_west.png`
  - `Rooms/attic_toy_storage_north.png`
  - `Rooms/attic_toy_storage_east.png`
  - `Rooms/attic_toy_storage_south.png`
  - `Rooms/attic_toy_storage_west.png`
  - `CloseUps/attic_family_album_photo.png`
  - `Puzzles/attic_toy_sequence.png`
  - `Items/item_small_doll.png`
  - `Items/item_symbol_fragment.png`
- Recently added first-floor laundry power resources:
  - `Rooms/stairwell_1f_north.png`
  - `Rooms/stairwell_1f_east.png`
  - `Rooms/stairwell_1f_south.png`
  - `Rooms/stairwell_1f_west.png`
  - `Rooms/first_floor_hallway_north.png`
  - `Rooms/first_floor_hallway_south.png`
  - `Rooms/dining_room_north.png`
  - `Rooms/dining_room_east.png`
  - `Rooms/dining_room_south.png`
  - `Rooms/dining_room_west.png`
  - `Rooms/kitchen_north.png`
  - `Rooms/kitchen_east.png`
  - `Rooms/kitchen_south.png`
  - `Rooms/kitchen_west.png`
  - `Rooms/laundry_room_north.png`
  - `Rooms/laundry_room_east.png`
  - `Rooms/laundry_room_south.png`
  - `Rooms/laundry_room_west.png`
  - `CloseUps/dining_seat_order_clue.png`
  - `CloseUps/kitchen_clock_clue.png`
  - `HideViews/kitchen_sink_hide_view.png`
  - `HideViews/laundry_machine_hide_view.png`
  - `Items/item_fuse.png`
  - `Items/item_old_keychain.png`

## Validation Status
- Verified with C# builds:
  - `dotnet build EscapeFromNightmares.csproj /p:UseSharedCompilation=false`
  - `dotnet build EscapeFromNightmares.Editor.csproj /p:UseSharedCompilation=false`
  - `dotnet build EscapeFromNightmares.EditModeTests.csproj /p:UseSharedCompilation=false`
- Unity Test Runner:
  - Previously succeeded before the latest lock issue.
  - Latest local check could not find `Unity.exe` in PATH or `C:\Program Files\Unity\Hub\Editor`, so batch EditMode tests were not run in this environment.
- Manual validation still needed in Unity Play Mode:
  - Title -> MainScene transition.
  - Four-face rotation.
  - Hidden world-object hitboxes remain clickable.
  - Drawer close-up fade, open, item pickup, empty state, and disabled room hitbox.
  - Hide view fade and back-arrow return.
  - Inventory window item icon display.
  - Child room -> second floor hallway transition.
  - Hallway two-screen left/right transition.
  - Hallway -> study entry.
  - Study four-face rotation.
  - Study clue close-up fade open/close.
  - Study safe puzzle fade open/close.
  - Safe digit images are visually consistent, aged, and not rendered as UI text.
  - `3-1-4-2` safe success opens the safe without immediately granting `fuse_holder`.
  - Open-safe close-up item hotspot grants `fuse_holder`.
  - Study north background changes to open safe with item, then open safe empty after pickup.
  - Study East exit returns to the hallway.
  - Hallway -> second floor bathroom entry.
  - Bathroom four-face rotation.
  - Bathroom mirror clue close-up fade open/close.
  - Bathroom East exit returns to the hallway.
  - Hallway -> dressing room entry.
  - Dressing room four-face rotation.
  - Dressing room color-sequence clue close-up fade open/close.
  - Dressing room East exit returns to the hallway.
  - Hallway -> mirror room entry.
  - Mirror room four-face rotation.
  - Mirror room symbol panel opens the puzzle close-up.
  - Mirror room East exit returns to the hallway.

## Next Recommended Work
- Manually QA the implemented `basement_altar_chain`, `final_chase_front_door_escape`, and `stage1_clear_ending_screen` route in Play Mode.
- After acceptance, tune `MonsterPlacementCatalog.asset` entries in the Unity Editor and continue with credits, final lore presentation, and any dedicated monster chase animation pass.
- The older QA-only and `master_bedroom_drawer` next-work notes below remain historical acceptance context.

## Current `monster_object_placement` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update + ScriptableObject asset + Generated bitmap assets + Tests`.
- Status: implemented as a first-pass runtime object placement harness.
- Runtime behavior:
  - The monster body is not baked into room background images.
  - `monster_shadow.png` is rendered as a separate `MonsterImage` UI object above the room object layer.
  - `MonsterPlacementCatalog.asset` is the source of truth for room/face placement coordinates.
  - `Disabled` and `Normal` hide the monster; `Approaching`, `Searching`, `NearDetection`, and `Chase` can show it when the current room/face entry is enabled and has a positive `normalizedRect`.
  - Development QA can toggle `MonsterRuntimeQaPanel` with `F9`, cycle monster states with `F10`, and toggle `Disabled/Normal` reset with `F11` in Editor/development builds.
- Placement data:
  - `MonsterPlacementCatalog.asset` excludes `child_room`.
  - Default entries are intentionally disabled and use `Rect.zero`; actual coordinates must be set in the Unity Editor.
  - Builder rebuilds preserve existing `enabled` and `normalizedRect` values for matching room/face entries.
- Assets and builder:
  - `Resources/EscapeFromNightmares/Monster/monster_shadow.png` is the shared transparent cutout sprite.
  - `RoomSpriteCatalog.asset` registers the sprite as `monster_shadow`.
  - `Escape From Nightmares/Rebuild Main Sample Assets` restores the catalog, resource registration, `MainScene` `MonsterImage`, and `MonsterRuntimeQaPanel` references.

## Current `stage1_clear_ending_screen` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update + Generated bitmap assets + Tests`.
- Status: implemented as a first-pass runtime harness with generated game-applied PNG asset.
- Runtime behavior:
  - `front_door_escape` still uses the existing complete-stage action path.
  - Stage clear applies `stage1_clear`, saves `clear_records.json` with `stage1Clear=true`, resets the monster, stops BGM, and shows a blocking `StageClearPanel`.
  - `StageClearPanel` displays `Stage Clear` and a `Title` button that returns to `TitleScene`.
- Generated image requirements:
  - Added `Endings/stage1_clear_background.png`.
  - Image is `1280x720`, generated as actual first-pass ending art, and contains no in-image text, UI, characters, or watermark.
  - Registered as `stage1_clear_background` in `RoomSpriteCatalog.asset` and `TitleSceneAssetBuilder`.
- Validation:
  - `dotnet build EscapeFromNightmares.csproj /p:UseSharedCompilation=false`: pass.
  - `dotnet build EscapeFromNightmares.Editor.csproj /p:UseSharedCompilation=false`: pass.
  - `dotnet build EscapeFromNightmares.EditModeTests.csproj /p:UseSharedCompilation=false`: pass.
  - Unity EditMode batch test could not run because another Unity instance has the project open.

## Current `final_chase_front_door_escape` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update + Generated bitmap assets + Tests`.
- Status: implemented as a first-pass runtime harness with generated game-applied PNG assets.
- Runtime behavior:
  - `basement_altar` still only sets `front_door_key_spawned`; it does not grant `front_door_key` or start the chase.
  - Picking up `front_door_key_on_altar` grants `front_door_key`, marks `basement_altar` solved, sets `event_front_door_key_appears`, starts `bgm_final_chase`, sets `final_chase_started`, and forces `MonsterState.Chase`.
  - Forced final chase remains sticky through `MonsterAIController.Tick()` until reset or stage clear.
  - Solving `front_door_escape` with `front_door_key` follows the existing complete-stage path and sets `stage1_clear`.
- Runtime room plan:
  - `entrance`: four faces; North contains transparent `front_door` puzzle hitbox, East exits to `first_floor_hallway`, South exits to `living_room`, West is ambient.
  - `first_floor_hallway` South and `entrance` North prefer chase-state backgrounds while `final_chase_started` is set.
- Generated image requirements:
  - Added `Rooms/entrance_north/east/south/west.png`.
  - Added `Rooms/entrance_north_chase.png` and `Rooms/first_floor_hallway_south_chase.png`.
  - Regenerated `Puzzles/front_door_escape.png` while preserving its existing `.meta` GUID.
  - All gameplay images are normalized to `1280x720`, live under `Assets/EscapeFromNightmares/Resources/EscapeFromNightmares/...`, import as Unity Sprites, and are registered in `RoomSpriteCatalog.asset` and `TitleSceneAssetBuilder`.
- EditMode tests:
  - Key pickup final chase state helper forces `MonsterState.Chase` and sets `final_chase_started`.
  - `basement_altar` solving alone does not start final chase.
  - `entrance` has the planned four-face front-door escape layout.
  - `front_door_escape` requires `front_door_key` and sets `stage1_clear`.
  - New final chase/front door assets load as sprites and are `1280x720`.
  - Conditional chase backgrounds win when `final_chase_started` is set.

## Current `basement_altar_chain` Harness
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update + ScriptableObject asset + Generated bitmap assets + Tests`.
- Status: implemented as a first-pass runtime harness with generated game-applied PNG assets.
- Goal:
  - Extend the playable route from `laundry_room` into `basement_entry`, `basement_main`, and `altar_room`.
  - Use `broken_hand_mirror`, `small_doll`, `old_keychain`, and `old_necklace` to solve `basement_altar`.
  - Spawn a visible `front_door_key_on_altar` pickup after the altar is solved.
  - Grant `front_door_key` from the altar pickup, but do not implement the final chase or front-door escape in this pass.
- Runtime room plan:
  - Update `LaundryRoom()` so the East face contains both `breaker_box_obj` and a `basement_entry_door`; gate the basement door behind `electricity_restored`.
  - Add dedicated `BasementEntry()`, `BasementMain()`, and `AltarRoom()` methods in `RuntimeStageFactory`.
  - `basement_entry`: four faces; North exits to `laundry_room`, East exits to `basement_main`, South/West ambient, `hideSpotCount = 0`.
  - `basement_main`: four faces; North opens `basement_wall_symbols` clue, East exits to `altar_room`, South exits to `basement_entry`, West opens `basement_main_hide`, `hideSpotCount = 1`.
  - `altar_room`: four faces; North contains `altar` and conditional `front_door_key_on_altar`, South exits to `basement_main`, East/West ambient, `hideSpotCount = 0`.
- Puzzle/item behavior:
  - Keep `basement_altar` answer order as `broken_hand_mirror -> small_doll -> old_keychain -> old_necklace`.
  - Set `basement_altar.oneShot = true`.
  - Change `basement_altar` so solving it sets `front_door_key_spawned` and does not immediately grant `front_door_key`.
  - Add a small runtime data hook for reward pickups, such as optional `solvesPuzzleId` on `InteractableDefinition`, so `front_door_key_on_altar` can grant `front_door_key`, mark `basement_altar` solved, and set `event_front_door_key_appears`.
  - Do not consume the four altar objects in this harness pass unless a later lore pass explicitly requires it.
- Generated image requirements:
  - Regenerate `Rooms/laundry_room_east.png` to show both the breaker box and the basement entry door/hatch; preserve the existing `.meta` GUID.
  - Add `Rooms/basement_entry_north/east/south/west.png`.
  - Add `Rooms/basement_main_north/east/south/west.png`.
  - Add `Rooms/altar_room_north/east/south/west.png`.
  - Add conditional altar room states `Rooms/altar_room_north_key_spawned.png` and `Rooms/altar_room_north_key_taken.png`.
  - Add `CloseUps/basement_wall_symbols.png` with the same symbol mapping used by the altar puzzle.
  - Regenerate or accept `Puzzles/basement_altar.png` as the actual altar puzzle panel art.
  - Add `HideViews/basement_main_hide_view.png`.
  - Add `Items/item_front_door_key.png`.
  - All new images must live under `Assets/EscapeFromNightmares/Resources/EscapeFromNightmares/...`, import as Unity Sprites, and be registered in `RoomSpriteCatalog.asset` and `TitleSceneAssetBuilder`.
- EditMode tests:
  - `laundry_room` exposes a gated `basement_entry_door` on the East face and keeps the breaker interaction.
  - `basement_entry`, `basement_main`, and `altar_room` have the planned four-face layouts and exit/clue/hide/puzzle placement.
  - All new room, close-up, puzzle, hide-view, conditional room-state, and item sprites load with `Resources.Load<Sprite>`.
  - `basement_altar` fails without all four altar objects.
  - Correct altar input sets `front_door_key_spawned`, does not immediately grant `front_door_key`, and cannot repeat rewards.
  - `front_door_key_on_altar` appears only after `front_door_key_spawned`, grants `front_door_key`, marks `basement_altar` solved, and does not repeat.
- Validation commands:
  - `dotnet build EscapeFromNightmares.csproj /p:UseSharedCompilation=false`
  - `dotnet build EscapeFromNightmares.Editor.csproj /p:UseSharedCompilation=false`
  - `dotnet build EscapeFromNightmares.EditModeTests.csproj /p:UseSharedCompilation=false`
  - `Unity.exe -batchmode -quit -projectPath . -runTests -testPlatform EditMode -testResults TestResults/EditMode.xml`
- Manual QA:
  - Acquire `broken_hand_mirror`, `small_doll`, `old_keychain`, and `old_necklace`.
  - Restore electricity in `laundry_room`, then verify the basement door becomes usable.
  - Move through `basement_entry -> basement_main -> altar_room`.
  - Open and close `basement_wall_symbols`.
  - Open `basement_altar`, solve it, verify `front_door_key_on_altar` appears in the room art, then pick up `front_door_key`.
  - Verify final chase visuals, monster pursuit, and `front_door_escape` remain deferred.

## Room Image Harness
- `docs/ROOM_IMAGE_HARNESS.md` is the required checklist for generating or accepting four-face room images.
- Data-level coverage now checks that every four-face room with exactly one connected room exposes exactly one exit-door interactable on exactly one face.
- Current 4-face room image QA:
  - `second_floor_bathroom`: North regenerated, East pass, South regenerated, West regenerated.
  - `dressing_room`: North regenerated, East pass, South pass, West regenerated.
  - `mirror_room`: North regenerated, East pass, South regenerated, West pass.
  - `master_bedroom`: North regenerated, East pass, South pass, West pass with portrait-as-painting note.
- Regeneration reasons:
  - Non-exit faces that showed extra door-like shapes were replaced.
  - `mirror_room_south` was replaced to remove character/silhouette-like mirror content.
  - `master_bedroom_north` was replaced to keep the color drawer puzzle wall without an extra exit-door shape.
- Asset size check: all 16 current room face PNGs are normalized to `1280x720`.
- QA status: all 16 current room face images pass the harness at first-pass art quality.

## Strict Close-Up Identity QA
- Superseding update: after the AI-generated replacement pass, strict identity for the four recorded crop pairs is intentionally `Exception`, not `Pass`; any older Pass wording below is historical context from the crop-derived pass.
- `docs/ROOM_IMAGE_HARNESS.md` now defines the strict close-up rule: a close-up passes only if it is a crop/enlargement of the same room face pixels, not a separately generated consistent image.
- Runtime/data coverage checks that face interactables with clue, object-state, or puzzle close-up resources load as sprites and are placed on only one face.
- Current strict identity result: Exception for the four previously crop-derived pairs because they were replaced by new AI-generated images; older child/study pairs remain Fail or out of this pass until rebuilt from recorded room-face crops.
- AI-generated strict identity exceptions:
  - `second_floor_bathroom_north` -> `bathroom_mirror_rule_clue`, crop `Rect(0.26, 0.24, 0.48, 0.48)`.
  - `dressing_room_north` -> `dressing_color_sequence_clue`, crop `Rect(0.22, 0.18, 0.56, 0.56)`.
  - `mirror_room_north` -> `mirror_symbol_panel`, crop `Rect(0.32, 0.13, 0.36, 0.36)`.
  - `master_bedroom_north` -> `master_bedroom_drawer`, crop `Rect(0.36, 0.20, 0.48, 0.48)`.
- Reviewed pairs:
  - `child_room`: desk drawer states, desk surface, drawing board, and window view are independently framed close-ups rather than proven room-face crops. `child_room_north_drawer_empty` also has a dark rectangular overlay artifact and needs cleanup before final art acceptance.
  - `study`: safe locked/open states, safe surrounding, clue note, desk surface, clue board, portrait, and window view are consistent with their room faces, but not strict crop-derived enlargements.
  - `second_floor_bathroom`, `dressing_room`, `mirror_room`, and `master_bedroom`: selected close-up/puzzle images are now AI-generated replacements, so strict identity is intentionally not satisfied.
- Required next asset fix for this stricter rule: rebuild older child/study close-up images from recorded source crops, starting with the `child_room_north_drawer_empty` artifact cleanup.

## AI Close-Up/Puzzle Replacement QA
- Objective changed from strict crop identity to new AI-generated close-up/puzzle art. Strict identity pairs are now intentional `Exception` cases because the replacements are independently generated art.
- Generated/Replaced/Pass at `1280x720`:
  - `CloseUps/bathroom_mirror_rule_clue.png`
  - `CloseUps/child_desk_drawer_closed.png`
  - `CloseUps/child_desk_drawer_open_empty.png`
  - `CloseUps/child_desk_drawer_open_with_item.png`
  - `CloseUps/child_desk_surface.png`
  - `CloseUps/child_drawing_board.png`
  - `CloseUps/child_window_view.png`
  - `CloseUps/dressing_color_sequence_clue.png`
  - `CloseUps/study_clue_board.png`
  - `CloseUps/study_desk_surface.png`
  - `CloseUps/study_portrait.png`
  - `CloseUps/study_safe_clue_note.png`
  - `CloseUps/study_safe_locked.png`
  - `CloseUps/study_safe_open_empty.png`
  - `CloseUps/study_safe_open_with_item.png`
  - `CloseUps/study_safe_surrounding.png`
  - `CloseUps/study_window_view.png`
  - `Puzzles/attic_toy_sequence.png`
  - `Puzzles/basement_altar.png`
  - `Puzzles/breaker_box.png`
  - `Puzzles/front_door_escape.png`
  - `Puzzles/laundry_storage_box.png`
  - `Puzzles/master_bedroom_drawer.png`
  - `Puzzles/mirror_symbol_panel.png`
  - `Puzzles/study_safe.png`
- Generated/Replaced/Pass at `180x220`:
  - `Puzzles/study_safe_digit_0.png` through `Puzzles/study_safe_digit_9.png`
- QA notes:
  - Digit sprites show exactly one matching Arabic numeral per file and no additional text.
  - General generated assets keep the dark gothic 2D escape-room style and existing runtime resource paths.
  - Existing `.meta` files were preserved; only PNG contents were replaced.

## Historical Work Lock: AI Close-Up/Puzzle Runtime Acceptance
- Deliverable classification: `Documentation QA + Unity validation`; only add `Script/Test` changes if validation finds a concrete defect.
- Goal:
  - Accept the newly generated close-up, puzzle, and safe digit images in the actual Unity runtime.
  - Confirm that existing runtime rules, puzzle answers, rewards, room connections, and hitboxes still work with the new art.
  - Identify any images that need targeted regeneration or hitbox retuning.
- Required Unity setup:
  - Close duplicate Unity editor instances before running batch tests or importing assets.
  - Let Unity import the replaced PNGs while preserving the existing `.meta` GUIDs.
  - Do not run `Rebuild Strict Identity Closeups` unless intentionally reverting the four AI-generated exception assets back to crop-derived images.
- Validation commands:
  - `dotnet build EscapeFromNightmares.csproj /p:UseSharedCompilation=false`
  - `dotnet build EscapeFromNightmares.EditModeTests.csproj /p:UseSharedCompilation=false`
  - Unity EditMode tests with no project lock: `Unity.exe -batchmode -quit -projectPath . -runTests -testPlatform EditMode -testResults TestResults/EditMode.xml`
- Manual QA checklist:
  - Open every close-up and puzzle modal once from normal room interaction, not only from direct resource preview.
  - Verify safe digit sprites `0` through `9` are centered, readable, visually consistent, and still fit the digit buttons.
  - Verify `study_safe` still solves with `3-1-4-2`, opens the safe close-up, and grants `fuse_holder` only from the item hotspot.
  - Verify `mirror_symbol_panel` still communicates `heart -> child_hand -> cracked_circle -> keyhole` and grants `broken_hand_mirror`.
  - Verify `master_bedroom_drawer` still communicates `black -> white -> red -> gray` and grants `old_necklace`.
  - Verify drawer/safe open-with-item and open-empty close-ups make item pickup state visually clear.
  - Record any mismatch as either `Regenerate image`, `Retune hitbox`, or `Runtime bug`; do not silently change puzzle rules to match art.
- Acceptance criteria:
  - All 35 replaced PNGs load as sprites in Unity.
  - General close-up/puzzle images remain `1280x720`; safe digit images remain `180x220`.
  - No new readable labels, unintended extra numbers, characters, watermarks, or UI-like marks appear in non-digit assets.
  - Strict identity status remains `Exception` for the four AI-generated replacement pairs.

## Historical Work Lock: `master_bedroom_drawer`
- Deliverable classification: `Script + Scene/Builder + ResourceCatalog update`.
- Goal:
  - Connect the existing dressing room color clue to the existing `master_bedroom_drawer` color-sequence puzzle.
  - Keep the answer unchanged: `black -> white -> red -> gray`.
  - Use real generated game images, not placeholder art.
- Expected room harness:
  - Replace the current simple `master_bedroom` definition with a dedicated four-face `MasterBedroom()` method.
  - North: color drawer puzzle area with `master_drawer_obj`.
  - East: exit door back to `second_floor_hallway`.
  - South: bed/closet ambience and one future hide location candidate, but do not lock the hide spot until the visual is confirmed.
  - West: moonlit personal objects or portrait/storage ambience with no required interaction in the first pass.
- Expected image resources:
  - `Rooms/master_bedroom_north.png`
  - `Rooms/master_bedroom_east.png`
  - `Rooms/master_bedroom_south.png`
  - `Rooms/master_bedroom_west.png`
  - `Puzzles/master_bedroom_drawer.png`
- Puzzle behavior for this pass:
  - `master_drawer_obj` opens the existing `master_bedroom_drawer` puzzle panel.
  - Successful solve keeps the existing reward path: grants `old_necklace` and sets `master_drawer_opened`.
  - Do not add drawer open/empty close-up states yet unless the next implementation explicitly scopes that in.
- Tests to add:
  - `master_bedroom` has four directional faces.
  - North face contains `master_drawer_obj`; East face contains `master_exit`.
  - `master_drawer_obj` resolves to `OpenPuzzle` with value `master_bedroom_drawer`.
  - Four room sprites and `Puzzles/master_bedroom_drawer` load through `Resources.Load<Sprite>`.
  - Hallway South face still links to `master_bedroom`.
- Manual QA:
  - Hallway South -> master bedroom entry.
  - Four-face rotation.
  - North drawer panel opens the color-sequence puzzle.
  - Correct color order solves the puzzle and grants `old_necklace`.
  - East exit returns to the hallway.
- Scope notes:
  - Final bedroom hide spot, monster behavior, drawer open-state art, and story/lore details are not locked in this next pass.
  - If new images are generated, preserve existing `.meta` GUIDs where files already exist; create sprite importer `.meta` only for new assets.
